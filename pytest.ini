[pytest]
testpaths = tests
markers =
    unit: fast deterministic unit tests
    integration: cross-module integration tests
    ollama: tests that require a live Ollama service
    slow: tests that may take longer than normal
addopts = -ra
